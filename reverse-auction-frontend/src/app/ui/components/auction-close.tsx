import CountdownTimer from "./countdown_timer";

const AuctionClose = () => {
  return (
    <div>
      <div className="flex justify-center items-center p-4 py-7 bg-beige rounded-lg shadow-md">
        Auction is now closed
      </div>
    </div>
  );
};

export default AuctionClose;
